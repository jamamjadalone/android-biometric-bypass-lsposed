<?xml version="1.0" encoding="utf-8"?>
<resources>
<!-- v2.1: EMPTY default scope. So apps are NOT pre-checked / auto-added to the
     LSPosed scope. The module only loads into apps the user manually selects
     in the in-app UI (MainActivity writes the scope via "lspd cli scope set"). -->
<string-array name="xposed_scope">
    </string-array>
</resources>